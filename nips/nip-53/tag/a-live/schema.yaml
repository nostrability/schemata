$schema: "https://json-schema.org/draft/2020-12/schema"
allOf:
  - $ref: "@/tag.yaml"
  - type: array
    minItems: 2
    items:
      - const: "a"
      - type: string
        pattern: '^30311:[a-f0-9]{64}:.+$'
      - type: string
        pattern: '^(ws://|wss://).+$'
    additionalItems: true

