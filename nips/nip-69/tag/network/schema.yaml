$schema: "http://json-schema.org/draft-07/schema#"
allOf:
  - $ref: "@/tag.yaml"
  - type: array
    minItems: 2
    maxItems: 2
    items:
      - const: "network"
      - type: string
        enum: ["mainnet", "testnet", "signet"]
    additionalItems: false
