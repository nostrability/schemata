$schema: http://json-schema.org/draft-07/schema#
title: kind9009
description: "Create invite event defined by NIP-29"
allOf:
  - $ref: "@/note.yaml"
  - type: object
    properties:
      kind:
        const: 9009
      tags:
        type: array
        items:
          $ref: "@/tag.yaml"
        allOf:
          - contains:
              $ref: "@/tag/h.yaml"
          - contains:
              $ref: "@/tag/code.yaml"
    required:
      - tags
